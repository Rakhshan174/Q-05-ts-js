/* 
Repeat Exercise 4, but this time store the famouse person's name in a variable called
famouse_person. Then compose your message and store it in a new variable called message. Print
your message.
*/
const famousPerson: String = " Albert Einstein";

const message: string = `${famousPerson} once said," A Person who never tried anything new."`;
console.log(message);